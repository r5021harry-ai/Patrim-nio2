# Módulo de Telas (views) da aplicação
# Renomeado de "pages" para "views" de propósito: uma pasta chamada
# "pages" ao lado do app.py faz o Streamlit criar automaticamente
# uma navegação extra na sidebar para cada arquivo aqui dentro,
# o que conflita com a navegação por abas feita manualmente no app.py.
